PORT=3001
ALLOWED_ORIGIN=https://mdac-better.vercel.app

# Optional: 2Captcha human-backed CAPTCHA fallback.
# When set, the auto-submit job escalates the final solver attempt to 2Captcha
# (CoordinatesTask) instead of the local Jimp heuristic. Leave blank to disable
# the fallback (local solver only). ~$0.001/solve. Get a key at 2captcha.com.
TWOCAPTCHA_API_KEY=
