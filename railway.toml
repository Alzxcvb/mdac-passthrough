[build]
builder = "nixpacks"

[deploy]
startCommand = "npx playwright install chromium --with-deps && node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
